# 协作规范（Contributing）

本仓库由多人共同编辑，请遵守以下规则，避免代码冲突、保护正式版本。

## 分支策略

- `master`：正式可用版本，建议在 Gitee 中开启分支保护，禁止直接推送
- 新功能分支：`feature/功能名`
- 修复分支：`fix/问题描述`
- 文档分支：`docs/说明`

## 提交信息格式

统一使用前缀，方便追溯：

- `feat:` 新功能
- `fix:` 修复 Bug
- `docs:` 文档改动
- `style:` 格式调整
- `refactor:` 重构
- `test:` 测试

示例：`feat: 增加论文章节生成接口`

## 工作流程

1. 开始工作前先同步最新代码：`git pull`
2. 新建自己的分支并开发
3. 本地提交，推送到自己的分支
4. 在 Gitee 上发起 Pull Request，指定负责人审核
5. 合并前解决冲突，合并后删除已合并的分支

## 注意事项

- 不要把 `.env`、API 密钥、token 等敏感信息提交到仓库
- 大文件不要直接入库，可使用 Gitee LFS 或外部网盘
- 修改他人负责的模块前，先在 Issue 或群里沟通
- 每次提交只做一件事，方便 Review 和回滚
